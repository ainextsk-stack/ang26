# SDK Site (Angular 21, standalone)
## Run
```bash
npm install
npm start
```
Open http://localhost:4200/

## Features
- Front page at `/`
- Canvas hero particles
- Theme toggle (light/dark) with localStorage
- VIS button: 6 skins via `data-skin` + localStorage
- "Подробнее" buttons with toast
