{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "files": [
    "src/main.ts"
    // "src/polyfills.ts"   // если файл существует в src/
  ],
  "include": [
    "src/**/*.d.ts"
  ],
  "exclude": [
    "**/*.spec.ts",      // чтобы тесты не мешали
    "src/test.ts"
  ]
}